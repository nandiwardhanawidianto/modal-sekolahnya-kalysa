import {useEffect,useState} from 'react';import {BrowserRouter,Navigate,Route,Routes,useLocation} from 'react-router-dom';import {api} from './lib/api';import Layout from './components/Layout';import Login from './pages/Login';import Dashboard from './pages/Dashboard';import MiaDashboard from './pages/MiaDashboard';import StoreReport from './pages/StoreReport';import Products from './pages/Products';import Imports from './pages/Imports';import Ads from './pages/Ads';import Cashflow from './pages/Cashflow';import Closings from './pages/Closings';import Stores from './pages/Stores';import Account from './pages/Account';import Loading from './components/Loading';import DataHealth from './pages/DataHealth';
function Root(){const[user,setUser]=useState(undefined);const[stores,setStores]=useState([]);const[storeId,setStoreIdState]=useState(localStorage.getItem('kalysa_store_id')||'');const loc=useLocation();const setStoreId=v=>{setStoreIdState(v);v?localStorage.setItem('kalysa_store_id',v):localStorage.removeItem('kalysa_store_id')};async function reloadStores(){const d=await api('/api/stores');setStores(d.stores||[]);if(!storeId&&d.stores?.[0])setStoreId(String(d.stores[0].id));return d}useEffect(()=>{api('/api/me').then(async d=>{setUser(d.user);if(d.user)await reloadStores()}).catch(()=>setUser(null))},[]);if(user===undefined)return <Loading text="Membuka aplikasi..."/>;if(!user)return loc.pathname==='/login'?<Login onLogin={async u=>{setUser(u);await reloadStores()}}/>:<Navigate to="/login" replace/>;return <Layout user={user} stores={stores} storeId={storeId} setStoreId={setStoreId}><Routes><Route path="/" element={<Dashboard/>}/><Route path="/mia" element={<MiaDashboard stores={stores}/>}/><Route path="/report" element={<StoreReport storeId={storeId}/>}/><Route path="/products" element={<Products storeId={storeId}/>}/><Route path="/imports" element={<Imports storeId={storeId}/>}/><Route path="/health" element={<DataHealth storeId={storeId}/>}/><Route path="/ads" element={<Ads storeId={storeId}/>}/><Route path="/cashflow" element={<Cashflow storeId={storeId}/>}/><Route path="/closings" element={<Closings storeId={storeId}/>}/><Route path="/stores" element={<Stores stores={stores} reloadStores={reloadStores} setStoreId={setStoreId}/>}/><Route path="/account" element={<Account/>}/><Route path="*" element={<Navigate to="/"/>}/></Routes></Layout>}
export default function App(){return <BrowserRouter><Root/></BrowserRouter>}
