import {useState} from 'react';import {api} from '../lib/api';
export default function Account(){const[current,setCurrent]=useState('');const[password,setPassword]=useState('');const[confirm,setConfirm]=useState('');const[msg,setMsg]=useState('');async function save(e){e.preventDefault();try{await api('/api/password',{method:'POST',body:{current_password:current,password,password_confirmation:confirm}});setMsg('Password berhasil diganti.');setCurrent('');setPassword('');setConfirm('')}catch(e){setMsg(e.message)}}return <><div className="page-head"><div><h2>Akun</h2><p>Password awal hanya untuk bootstrap. Masing-masing user bisa mengganti password sendiri.</p></div></div>{msg&&<div className="info-box">{msg}</div>}<form className="card" onSubmit={save} style={{maxWidth:520}}><label>Password sekarang<input type="password" value={current} onChange={e=>setCurrent(e.target.value)} required/></label><label>Password baru<input type="password" value={password} onChange={e=>setPassword(e.target.value)} minLength="10" required/></label><label>Ulangi password baru<input type="password" value={confirm} onChange={e=>setConfirm(e.target.value)} minLength="10" required/></label><button className="btn primary">Ganti Password</button></form></>}
