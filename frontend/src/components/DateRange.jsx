export default function DateRange({start,end,onChange,onSubmit,loading}){return <form className="date-range" onSubmit={e=>{e.preventDefault();onSubmit?.()}}><label>Dari<input type="date" value={start} onChange={e=>onChange('start',e.target.value)}/></label><label>Sampai<input type="date" value={end} onChange={e=>onChange('end',e.target.value)}/></label><button className="btn primary" disabled={loading}>Tampilkan</button></form>}
